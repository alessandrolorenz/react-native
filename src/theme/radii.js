// Border radius scale. `nested` (12) is sized for inner-frame borders that
// sit inside a `lg` (24) container — see ItemProfileScreen hero.

export const radii = {
  xs: 4,
  sm: 8,
  nested: 12,
  md: 16,
  lg: 24,
  pill: 999,
};
